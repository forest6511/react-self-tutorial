var expect = require('expect');

it('should properly run test', () => {
    expect(1).toBe(1);
});

describe('App', () => {
    it('should properly run test', () => {
        expect(1).toBe(1);
    });
});